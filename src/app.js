import 'bootstrap'
import '@popperjs/core'
import Typed from 'typed.js'

new Typed('.typed-string', {
  stringsElement: '#typed-strings',
  typeSpeed: 20,
  startDelay: 1000,
  backDelay: 2000,
  loop: true
})
